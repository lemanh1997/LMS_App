<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? %>
    | <%= link_to t(:text_edit), edit_user_path(user) %>
    | <%= link_to t(:text_delete), user, method: :delete %>
  <% end %>
</li>
