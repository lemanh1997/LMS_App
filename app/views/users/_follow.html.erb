<%= form_for(current_user.active_relationship_user.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= f.submit t(:text_follow), class: "btn btn-primary" %>
<% end %>
