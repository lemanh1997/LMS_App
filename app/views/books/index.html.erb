<% provide(:title, t("books.index.name_page")) %>

<h1><%= t("books.index.name_page") %></h1>

<div class="row">
  <%= render "search" %>
  <%= render "export" %>
  <div class="col-md-2" >
    <% if current_user.admin? %>
      <%= link_to t("books.index.text_button"), new_book_path, class: "btn btn-outline-primary rights" %>
    <% end %>
  </div>
</div>

<ul class="objects">
  <%= render @books %>
</ul>

<%= will_paginate %>
