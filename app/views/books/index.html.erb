<% provide(:title, t("books.index.name_page")) %>

<h1><%= t("books.index.name_page") %></h1>

<%= link_to t("books.index.text_button"), new_book_path %>

<%= will_paginate %>

<ul class="books">
  <%= render @books %>
</ul>

<%= will_paginate %>
