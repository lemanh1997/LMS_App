<li>
  <%= link_to category.name, category %>
  <% if current_user.admin? %>
    | <%= link_to t(:text_edit), edit_category_path(category) %>
    | <%= link_to t(:text_delete), category, method: :delete %>
  <% end %>
</li>
